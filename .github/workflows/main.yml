name: Moon Web Tests

on:
  workflow_dispatch:
  schedule:
    - cron: '0 3 * * *'

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Start Moon2
        run: |
          # Moon2 сам поймет, что он в Docker, если прокинуть сокет
          docker run -d --name moon \
            -p 4444:4444 \
            -v /var/run/docker.sock:/var/run/docker.sock \
            aerokube/moon2:latest
          
          # Даем 15 секунд на прогрев
          sleep 15
          
          echo "--- MOON2 LOGS ---"
          docker logs moon
          
          echo "--- CHECKING CONNECTION ---"
          curl -I http://localhost:4444/status

      - name: Setup JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Run tests
        run: mvn test
